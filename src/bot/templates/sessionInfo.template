🏎 <b>{{sessionInfo.race.meetingOfficialName.toUpperCase()}}</b> 🏎

<b>🇺🇸 {{sessionInfo.description.toUpperCase()}} STARTS IN {{Math.floor(sessionInfo.msToGo / 60000)}} MINUTES 🇺🇸</b>
<b>🇪🇸 {{translate('es-ES', sessionInfo.description).toUpperCase()}} EMPIEZA EN {{Math.floor(sessionInfo.msToGo / 60000)}} MINUTOS 🇪🇸</b>